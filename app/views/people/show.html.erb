<%- model_class = Person -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.titleize %></h1>
</div>

<table  class="table table-hover table-striped table-responsive">
  <caption></caption>
  <tr>
    <td><strong><%= model_class.human_attribute_name(:last_name) %></td>
    <td><%= @person.last_name %></td>
  </tr>
  <tr>
    <td><strong><%= model_class.human_attribute_name(:first_name) %></td>
    <td><%= @person.first_name %></td>
  </tr>
  <tr>
    <td><strong><%= model_class.human_attribute_name(:email) %></td>
    <td><%= mail_to @person.email %></td>
  </tr>
  <tr>
    <td><strong><%= model_class.human_attribute_name(:telefon_fest) %></td>
    <td><%= @person.telefon_fest %></td>
  </tr>
  <tr>
    <td><strong><%= model_class.human_attribute_name(:telefon_mobil) %></td>
    <td><%= @person.telefon_mobil %></td>
  </tr>
  <tr>
    <td><strong><%= model_class.human_attribute_name(:geburtsdatum) %></td>
    <td><%=l @person.geburtsdatum %></td>
  </tr>
  <% if @current_user.isAdmin? %>
      <tr>
        <td><strong><%= model_class.human_attribute_name(:user_id) %></td>
        <td><%= @person.user_id %></td>
      </tr>
  <% end %>
</table>

<div class="form-actions">
  <%= link_to t('.back', :default => t("helpers.links.back")),
              user_people_path, :class => 'btn'  %>
  <%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_user_person_path(@person.user_id, @person.id), :class => 'btn' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              user_person_path(@person.user_id, @person.id),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
</div>
